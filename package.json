{
  "name": "3dollars-in-my-pocket-link",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "copy-aasa": "node scripts/copy-aasa.js",
    "dev": "VITE_ENV=dev npm run copy-aasa && vite --mode development",
    "prod": "VITE_ENV=prod npm run copy-aasa && vite --mode production",
    "build": "VITE_ENV=dev npm run copy-aasa && tsc && vite build --mode development",
    "build:dev": "VITE_ENV=dev npm run copy-aasa && tsc && vite build --mode development",
    "build:prod": "VITE_ENV=prod npm run copy-aasa && tsc && vite build --mode production",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-router-dom": "^7.9.4"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.0.0",
    "vite": "^5.2.0"
  }
}
