{
  "name": "3dollars-in-my-pocket-link",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "copy-aasa": "node scripts/copy-aasa.js",
    "dev": "npm run copy-aasa && vite",
    "build": "npm run copy-aasa && tsc && vite build",
    "build:dev": "VITE_ENV=dev npm run build",
    "build:prod": "VITE_ENV=prod npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.0.0",
    "vite": "^5.2.0"
  }
}
